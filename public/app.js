const events=[
 {date:'COMING SOON',title:'Paint & Sip with Janice',host:'Hosted by Janice',desc:'Janice has taught art for 22 years and recently brought her experience to a relaxed Paint & Sip setting. After two successful BLP gatherings, this will be event number three.',spots:'Limited spots',cta:'Reserve your spot'},
 {date:'YOUR DATE HERE',title:'Could this be your workshop?',host:'Hosted by you',desc:'Teach the class, test the concept, meet your first customers. Start with a small room full of the right people and learn what works.',spots:'Up to 20 guests',cta:'Pitch your idea'},
 {date:'SEASONAL',title:'Something new is always launching',host:'Murfreesboro, TN',desc:'From wellness mornings and creative workshops to tastings, clubs and pop-ups — BLP is built for experiences that deserve somewhere to begin.',spots:'Stay tuned',cta:'See event ideas'}
];
const grid=document.querySelector('#eventGrid');
events.forEach((e,i)=>{const a=document.createElement('article');a.className='event-card';a.innerHTML=`<div class="date">${e.date}</div><h3>${e.title}</h3><div class="hostline">${e.host}</div><p class="desc">${e.desc}</p><div class="card-foot"><span>${e.spots}</span><a href="${i===0?'#request':i===1?'#request':'#ideas'}">${e.cta} →</a></div>`;grid.appendChild(a)});
const form=document.querySelector('#inquiryForm');form.addEventListener('submit',async e=>{e.preventDefault();const status=document.querySelector('#formStatus');status.textContent='Sending…';try{const data=Object.fromEntries(new FormData(form));const r=await fetch('/api/inquiry',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const out=await r.json();status.textContent=out.message;form.reset()}catch(err){status.textContent='Something went wrong. Please try again.'}});